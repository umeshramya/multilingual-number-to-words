import { LAN, WORD } from "../../config/interfaces";
export default class DoubleDigits implements WORD {
    private _lan;
    constructor(lan: LAN);
    getWord(_number: number): string;
}
//# sourceMappingURL=doubleDigits.d.ts.map