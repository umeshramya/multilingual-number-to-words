import { LAN, WORD } from "../../config/interfaces";
export default class Single implements WORD {
    private _lan;
    constructor(lan: LAN);
    getWord(_number: number): string;
}
//# sourceMappingURL=singleDigits.d.ts.map