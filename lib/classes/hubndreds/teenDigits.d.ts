import { LAN, WORD } from "../../config/interfaces";
export default class TeenDigits implements WORD {
    private _lan;
    constructor(lan: LAN);
    getWord(_number: number): string;
}
//# sourceMappingURL=teenDigits.d.ts.map